# CIRCULARITY-CONFIRMATION EXPERIMENT — SCIENTIFIC CONTROL ONLY.
# ⚠️  THIS CONFIG VIOLATES INVARIANT 1 ⚠️
# See DESIGN.md §0 change P10. Default OFF; only enabled here as a control.
# @package _global_
defaults:
  - /config
  - /model: model_msr_hine
  - _self_

model:
  name: "msr_hine"
  _inference_fusion_CONTROL_ONLY: true

train:
  lambda_prior: 0.1
  lambda_cons: 0.1
  lambda_spec: 0.01
  lambda_highk: 0.0
  scheduled_sampling:
    start_prob: 1.0
    end_prob: 0.0
